import { Layout } from "@/components/layout/Layout";

const Ecommerce = () => {
  return (
    <Layout>
      <section className="flex min-h-[calc(100vh-5rem)] items-center justify-center bg-background px-6">
        <h1 className="heading-1 text-center text-foreground">Work in Progress</h1>
      </section>
    </Layout>
  );
};

export default Ecommerce;
